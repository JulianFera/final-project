export type ThemeType = "light" | "dark";

export type AllMoviesType = {
  id: number;
  title: string;
  poster_path: string;
};

export type PopularMoviesType = {
  id: number;
  poster_path: string;
};
